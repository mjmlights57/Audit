# EWPros v3.0.3 Upgrade

1. Keep the working VPS deployment unchanged while testing.
2. In Supabase SQL Editor, run `EWPROS-V3.0.3-MIGRATION.sql` once. It is additive and preserves historical records.
3. Upload the v3.0.3 application files to GitHub and allow Netlify to deploy.
4. Test `/admin.html`:
   - Banking: select a CSV; **Import transactions** should become enabled immediately.
   - Preview remains optional.
   - Accounting: verify the requested category list is present and `Labor` / old `Subcontractor` are absent from active choices.
5. Only after Netlify testing passes, update the VPS using the rollback workflow.
